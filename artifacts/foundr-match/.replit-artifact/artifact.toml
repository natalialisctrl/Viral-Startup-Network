kind = "web"
previewPath = "/"
title = "FoundrMatch"
version = "1.0.0"
id = "artifacts/foundr-match"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 8081

[services.development]
run = "pnpm --filter @workspace/foundr-match run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/foundr-match", "run", "build" ]
publicDir = "artifacts/foundr-match/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "8081"
BASE_PATH = "/"
